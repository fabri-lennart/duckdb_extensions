# DuckDB Extensions Playground

Practice repo for DuckDB extensions.

## Structure
- `data/` - Input files
- `queries/` - SQL files
- `outputs/` - Results and screenshots

## Usage
```bash
duckdb < queries/your_query.sql
```

## Extensions
httpfs, parquet, json, fts, sqlite, spatial, and more.
